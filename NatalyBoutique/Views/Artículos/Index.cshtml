@model IEnumerable<NatalyBoutique.Models.Artículos>

@{
    ViewData["Title"] = "Index";

}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CantidadExistencia)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CantidadExistencia)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdArticulo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdArticulo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdArticulo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
